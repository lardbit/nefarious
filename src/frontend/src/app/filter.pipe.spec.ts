import { MediaFilterPipe } from './filter.pipe';

describe('MediaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
